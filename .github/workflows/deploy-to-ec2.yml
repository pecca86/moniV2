name: Deploy to EC2 K3s Cluster

on:
  workflow_run:
    workflows: ["Build and Push Docker Images to ECR"]
    types:
      - completed
    branches: [main]
  # Allow manual trigger
  workflow_dispatch:

env:
  AWS_REGION: eu-central-1
  ECR_REGISTRY: 026596707189.dkr.ecr.eu-central-1.amazonaws.com
  ECR_REPOSITORY_BACKEND: moni-be
  ECR_REPOSITORY_FRONTEND: moni-fe

jobs:
  deploy-to-ec2:
    name: Deploy BE/FE to EC2 K3s (Preserve DB)
    runs-on: ubuntu-latest
    # Only run if the build workflow succeeded (or manual trigger)
    if: ${{ github.event.workflow_run.conclusion == 'success' || github.event_name == 'workflow_dispatch' }}
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Configure AWS credentials
      uses: aws-actions/configure-aws-credentials@v4
      with:
        aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
        aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
        aws-region: ${{ env.AWS_REGION }}

    - name: Get EC2 Instance IP
      id: get-ip
      run: |
        INSTANCE_IP=$(aws ec2 describe-instances \
          --filters "Name=tag:Name,Values=moni-dev-k3s" "Name=instance-state-name,Values=running" \
          --query "Reservations[0].Instances[0].PublicIpAddress" \
          --output text 2>/dev/null || echo "")
        
        if [ "$INSTANCE_IP" = "None" ] || [ -z "$INSTANCE_IP" ]; then
          # Fallback to secret if available
          if [ -n "${{ secrets.EC2_INSTANCE_IP }}" ]; then
            INSTANCE_IP="${{ secrets.EC2_INSTANCE_IP }}"
            echo "✅ Using EC2 IP from secrets: $INSTANCE_IP"
          else
            echo "❌ Could not find EC2 instance IP"
            exit 1
          fi
        else
          echo "✅ Found EC2 instance IP: $INSTANCE_IP"
        fi
        echo "instance_ip=$INSTANCE_IP" >> $GITHUB_OUTPUT

    - name: Setup SSH Key
      run: |
        mkdir -p ~/.ssh
        echo "${{ secrets.EC2_SSH_PRIVATE_KEY }}" > ~/.ssh/ec2_key
        chmod 600 ~/.ssh/ec2_key
        
        # Add EC2 host to known hosts (skip host key verification for CI)
        ssh-keyscan -H ${{ steps.get-ip.outputs.instance_ip }} >> ~/.ssh/known_hosts 2>/dev/null || true

    - name: Deploy to K3s on EC2
      env:
        EC2_IP: ${{ steps.get-ip.outputs.instance_ip }}
      run: |
        echo "🚀 Deploying to EC2 K3s cluster at $EC2_IP"
        
        # SSH to EC2 and run the deployment commands
        ssh -i ~/.ssh/ec2_key -o StrictHostKeyChecking=no ec2-user@$EC2_IP << 'DEPLOY_SCRIPT'
        set -e
        
        echo "📦 Step 1: Updating ECR credentials for containerd..."
        # K3s uses containerd, need to update the registries config with fresh ECR token
        ECR_TOKEN=$(aws ecr get-login-password --region eu-central-1)
        ECR_REGISTRY="026596707189.dkr.ecr.eu-central-1.amazonaws.com"
        
        # Create/update containerd registry config for ECR
        sudo mkdir -p /etc/rancher/k3s
        sudo tee /etc/rancher/k3s/registries.yaml > /dev/null << EOF
        mirrors:
          "$ECR_REGISTRY":
            endpoint:
              - "https://$ECR_REGISTRY"
        configs:
          "$ECR_REGISTRY":
            auth:
              username: AWS
              password: $ECR_TOKEN
        EOF
        
        echo "🔄 Step 2: Restarting Backend deployment (pulls new image)..."
        sudo kubectl rollout restart deployment/moni-be -n moni
        
        echo "🔄 Step 3: Restarting Frontend deployment (pulls new image)..."
        sudo kubectl rollout restart deployment/moni-fe -n moni
        
        echo "⏳ Step 4: Waiting for rollouts to complete..."
        sudo kubectl rollout status deployment/moni-be -n moni --timeout=300s
        sudo kubectl rollout status deployment/moni-fe -n moni --timeout=180s
        
        echo "✅ Step 5: Verifying deployments..."
        sudo kubectl get pods -n moni
        
        echo ""
        echo "🎉 Deployment complete! Database was NOT touched."
        DEPLOY_SCRIPT

    - name: Verify Deployment
      env:
        EC2_IP: ${{ steps.get-ip.outputs.instance_ip }}
      run: |
        echo "🔍 Verifying deployment health..."
        
        # Wait a moment for services to be ready
        sleep 10
        
        # Check backend health via NodePort
        HEALTH_STATUS=$(curl -s -o /dev/null -w "%{http_code}" http://$EC2_IP:30080/actuator/health || echo "000")
        
        if [ "$HEALTH_STATUS" = "200" ]; then
          echo "✅ Backend is healthy (HTTP 200)"
        else
          echo "⚠️  Backend health check returned: $HEALTH_STATUS"
          echo "This might be normal if the app is still starting up"
        fi
        
        # Check frontend
        FE_STATUS=$(curl -s -o /dev/null -w "%{http_code}" http://$EC2_IP:30081/ || echo "000")
        
        if [ "$FE_STATUS" = "200" ]; then
          echo "✅ Frontend is healthy (HTTP 200)"
        else
          echo "⚠️  Frontend health check returned: $FE_STATUS"
        fi

    - name: Create Deployment Summary
      env:
        EC2_IP: ${{ steps.get-ip.outputs.instance_ip }}
      run: |
        echo "## 🚀 Deployment Complete!" >> $GITHUB_STEP_SUMMARY
        echo "" >> $GITHUB_STEP_SUMMARY
        echo "### ✅ Deployed Services" >> $GITHUB_STEP_SUMMARY
        echo "| Service | Status | Image |" >> $GITHUB_STEP_SUMMARY
        echo "|---------|--------|-------|" >> $GITHUB_STEP_SUMMARY
        echo "| Backend | 🔄 Updated | \`$ECR_REGISTRY/$ECR_REPOSITORY_BACKEND:latest\` |" >> $GITHUB_STEP_SUMMARY
        echo "| Frontend | 🔄 Updated | \`$ECR_REGISTRY/$ECR_REPOSITORY_FRONTEND:latest\` |" >> $GITHUB_STEP_SUMMARY
        echo "| Database | ✅ Preserved | PostgreSQL 16 (data intact) |" >> $GITHUB_STEP_SUMMARY
        echo "" >> $GITHUB_STEP_SUMMARY
        echo "### 🌐 Access URLs" >> $GITHUB_STEP_SUMMARY
        echo "- **Frontend:** http://$EC2_IP:30081" >> $GITHUB_STEP_SUMMARY
        echo "- **Backend API:** http://$EC2_IP:30080" >> $GITHUB_STEP_SUMMARY
        echo "- **Health Check:** http://$EC2_IP:30080/actuator/health" >> $GITHUB_STEP_SUMMARY
        echo "" >> $GITHUB_STEP_SUMMARY
        echo "### 📝 Commit" >> $GITHUB_STEP_SUMMARY
        echo "Deployed from: \`${{ github.sha }}\`" >> $GITHUB_STEP_SUMMARY

    - name: Cleanup SSH Key
      if: always()
      run: |
        rm -f ~/.ssh/ec2_key
